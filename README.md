# .alex-config
Linux configuration files

Simply git clone this directory. Then you can run the following command to setup the environment automatically:

cd ~ && rm .vimrc && rm .bashrc && ln -s .alex-config/.vimrc .vimrc && ln -s .alex-config/.bashrc .bashrc 


